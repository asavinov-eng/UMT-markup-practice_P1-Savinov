{
  "name": "flora",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "api": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "json-server": "^0.17.4"
  }
}
